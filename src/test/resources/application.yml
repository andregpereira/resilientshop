# H2
spring:
  datasource:
    url: jdbc:h2:mem:resilientshop-shopping-test
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate.format_sql: true
    show-sql: true
